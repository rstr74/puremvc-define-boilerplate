// create the boilerplate namespace
window.boilerplate = {
    AppConstants : {

        // Notifications (always preface with NOTE_ to distinguish between other statics)
        NOTE_STARTUP : 'startup',
        NOTE_DO_SOMETHING : 'note_do_something',
        NOTE_DID_SOMETHING : 'note_did_something',
    }
};
